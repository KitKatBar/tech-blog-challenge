<section class="card">
  <a href="/post/{{post_id}}">
    <p>{{comment}}</p>
    <p>Posted on {{date_created}}</p>
  </a>

  <a href="/edit_comment/{{id}}"><button class="btn btn-primary" data-id="{{id}}">edit</button></a>
<button class="btn btn-primary delete-comment" data-id="{{id}}">delete</button>
</section>
